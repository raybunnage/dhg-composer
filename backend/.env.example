# Template environment file - copy this for different environments
SUPABASE_URL=your_url
SUPABASE_KEY=your_key

# Environment-specific URLs
FRONTEND_URL=https://baseline.dhg-hub.org  # Change per environment
API_URL=https://baseline-api.dhg-hub.org   # Change per environment
ENVIRONMENT=baseline                        # baseline/development/production 