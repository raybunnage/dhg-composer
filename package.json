{
  "name": "dhg-composer",
  "private": true,
  "workspaces": [
    "apps/dhg-baseline/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:frontend": "npm run dev -w dhg-baseline-frontend",
    "dev:backend": "npm run dev -w dhg-baseline-backend"
  },
  "devDependencies": {
    "turbo": "^1.11.2"
  }
}
